{
  "name": "purescript-optional",
  "version": "0.1.0",
  "description": "Optional is a newtype on the uncurried Church encoding of Maybe.",
  "keywords": ["ffi", "foreign", "optional", "null", "undefined", "purescript"],
  "author": "Eric Brisco",
  "license": "BSD-3-Clause",
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build -- --censor-lib --strict"
  },
  "devDependencies": {
    "pulp": "^10.0.4",
    "purescript-psa": "^0.5.0-rc.1",
    "rimraf": "^2.6.1"
  }
}
